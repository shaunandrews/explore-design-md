# WordPress VIP DESIGN.md — Review Backlog

Items flagged for human review. Confidence level indicates how sure the AI extraction was.

## High Priority (needs verification against live site)

- [ ] **Aktiv Grotesk VF exact weights used across pages** — Confidence: ~60%. DESIGN.md documents 700 for display/H1, 500 for H2-H6, and 400 for body. However, the actual weights used may vary across different page templates (homepage hero vs. product pages vs. blog). Aktiv Grotesk VF is a variable font, so intermediate weights (e.g., 600) could appear. Verify via DevTools on wpvip.com across at least 3 page types.

- [ ] **Colorful multi-stop gradient usage scope** — Confidence: ~55%. DESIGN.md describes colorful gradients mixing teal, green, peach, and coral for section backgrounds. It's unclear whether these apply only to the homepage/marketing pages or also to documentation, blog, and partner pages. The exact gradient angles and color stop positions may differ per section. Verify which pages use the colorful gradients vs. plain warm gradient surfaces.

- [ ] **Dark atmospheric gradient exact composition** — Confidence: ~65%. DESIGN.md mentions "deep atmospheric gradients mixing dark reds, teals, and greens on #13191e" but doesn't provide exact CSS values for the dark gradient overlays. The preview files use approximated radial-gradient values. Inspect the actual dark sections on wpvip.com for precise gradient definitions.

- [ ] **Navigation mega-menu structure and behavior** — Confidence: ~50%. DESIGN.md notes a mega-menu with product categories, solutions by industry/department, resources, and featured content cards. The exact panel layout, animation, column structure, and featured card placement need verification. Also unclear: does the frosted glass header (#f5f2f1bf) persist on scroll or change?

## Medium Priority (likely correct but worth checking)

- [ ] **Tag color completeness (9 variants)** — Confidence: ~80%. All 9 tag variants (gray, blue, green, red, gold, yellow, orange, salmon, pink) were extracted with background and icon colors. Verify these are all current variants — there may be additional variants used in the VIP Dashboard or documentation that aren't reflected on the marketing site.

- [ ] **Form field styling specifics** — Confidence: ~70%. DESIGN.md documents transparent background, 1px border (#bfbcbb), and blue focus ring (#0F62FE). However, the blue focus ring is unusual for a warm gold brand system — this may be a WordPress/Gutenberg inherited default rather than a deliberate WPVIP choice. Verify focus ring color and whether custom focus styles exist.

- [ ] **6-layer progressive shadow exact values** — Confidence: ~85%. The high shadow values (6 layers from 2.767px to 100px) were extracted from CSS. These are likely generated by a shadow tool/formula. Verify the rgba opacity values haven't changed in recent updates.

- [ ] **Aktiv Grotesk EX usage boundaries** — Confidence: ~75%. DESIGN.md specifies Extended width only for "support text and accent labels" at 12px and 16px. Verify this font isn't also used for navigation labels, tag text, or other UI elements that might benefit from the extended width.

- [ ] **Border-radius 40px vs 32px for cards** — Confidence: ~80%. DESIGN.md lists both 32px (large) and 40px (x-large) for cards. Verify which radius is dominant on standard content cards vs. featured/hero cards. The preview uses 32px as the default card radius.

- [ ] **Button disabled state color #827f7e** — Confidence: ~75%. This value was documented for primary button disabled state. Verify this specific gray is used rather than the general disabled token (#bfbcbb).

## Low Priority (minor details)

- [ ] **clamp() value precision** — Confidence: ~90%. All clamp values were extracted from CSS custom properties. These are mathematically derived and should be stable, but verify if any have been adjusted in recent design system updates.

- [ ] **AkkuratMonoLLWeb usage extent** — Confidence: ~85%. Documented for code blocks at 14px and 16px. Verify if this mono font is used elsewhere (e.g., data tables, metrics, technical documentation pages).

- [ ] **Layer Blur semi-transparency value** — Confidence: ~85%. Documented as #f5f2f1bf (75% opacity). Verify the exact alpha value used for the frosted glass navigation header — it may differ between mobile and desktop.

- [ ] **Link color scale completeness** — Confidence: ~80%. Three link states documented (#9a6014 default, #7a4909 hover, #522e03 active). Verify if visited links have a distinct color or reuse the active state.

- [ ] **Icon system secondary colors** — Confidence: ~80%. Icon colors documented (primary #13191e, secondary #6a6766, helper #9b9796, etc.) but these may vary in the VIP Dashboard product vs. the marketing site.

## Preview File Notes

- **Font substitution**: Preview files use **DM Sans** from Google Fonts as a stand-in for Aktiv Grotesk VF (commercial variable font). The actual site loads `Aktiv Grotesk VF`. If you have the font files, replace the Google Fonts import with local @font-face declarations.
- **Extended font**: Aktiv Grotesk EX (extended width) is also commercial. DM Sans is used as a fallback but does not replicate the extended width characteristic.
- **Monospace font**: AkkuratMonoLLWeb-Regular is commercial. DM Mono is used as a fallback in the preview files.
- **Logo**: Uses a text-based SVG placeholder. Replace with the actual WordPress VIP logo SVG for production use.
- **Dark gradients**: The atmospheric dark gradients in preview-dark.html are approximations. Exact gradient values should be verified against the live site.

## How to Review

1. Open wpvip.com in a browser
2. Use DevTools to inspect the elements mentioned above
3. Check the item off and note any corrections needed
4. Update the DESIGN.md with corrected values
5. Update preview.html / preview-dark.html if visual tokens change
